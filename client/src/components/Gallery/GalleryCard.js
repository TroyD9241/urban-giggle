export const GalleryCard = ({ post }) => {
  return (
    <>
      hi from the card
      <img
        class="block object-cover object-center w-full h-full rounded-lg"
        src={post}
      />
    </>
  );
};
